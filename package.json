{
  "name": "overflow-observer",
  "description": "A tiny web component to determine if an element is overflowing its container.",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "files": [
    "lib/mod.js"
  ],
  "module": "./lib/mod.js",
  "scripts": {
    "start": "web-dev-server --open --watch"
  },
  "dependencies": {
    "web-dev-server": "^3.0.27"
  }
}
